name: CI

on:
  push:
    branches: [ main, test]
  pull_request:
    branches: [ main, test]
  schedule:
    - cron: '0 0 * * 0'

jobs:
  test:
    name: Test (${{ matrix.os }} / Go ${{ matrix.go-version }})
    strategy:
      matrix:
        os: [ ubuntu-latest, macos-latest, windows-latest ]
        go-version: [ '1.24', '1.25', '1.26' ]
      fail-fast: false
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}
          cache: true

      - name: Run tests without coverage (Windows or Go 1.24)
        if: matrix.go-version == '1.24' || runner.os == 'Windows'
        run: go test -v -race ./...

      - name: Run tests with coverage (Linux/macOS, Go 1.25+)
        if: matrix.go-version != '1.24' && runner.os != 'Windows'
        run: |
          go test -v -race -cover -coverprofile=coverage.out ./...
          go tool cover -func=coverage.out

      - name: Test CLI
        run: |
          cd cli
          go mod tidy
          go test -v ./...

      - name: Upload coverage to Codecov
        if: matrix.go-version != '1.24' && matrix.os == 'ubuntu-latest'
        uses: codecov/codecov-action@v7
        continue-on-error: true
        with:
          files: ./coverage.out
          flags: unittests

  check-benchmarks:
    name: Check Benchmarks Compilation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache: true

      - name: Compile benchmarks
        run: go test -bench=. -benchtime=1x -run=^$ ./...

  lint-and-security:
    name: Quality & Security
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache: true

      - name: Static analysis (staticcheck)
        run: |
          go install honnef.co/go/tools/cmd/staticcheck@latest
          staticcheck ./...

      - name: Check vulnerabilities
        uses: golang/govulncheck-action@v1.0.4

      - name: Security scanner
        uses: securego/gosec@master
        with:
          args: '-exclude=G304 ./...'